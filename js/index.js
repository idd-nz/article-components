// DOCUMENT REFERENCES
let $doc = document.documentElement  // html element
let $win = window
let $bar = document.querySelector('.progress-bar')


// 1. Read Part 1 "Advanced" (Step 1 and 2) and determine which elements need to be selected in addition to the three above

// 2. Read Step 2 closely and break the request into as many 1-line parts as possible (write the process as a JS comment below)

