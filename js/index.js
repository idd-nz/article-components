// DOCUMENT REFERENCES
let $doc =    document.documentElement  // html element
let $win =    window
let $bar =    document.querySelector('.bar')
let $prog =   document.querySelector('.progress')
let $h1 =     document.querySelector('#heading')
let $h1copy = document.querySelector('.h1copy')


// STEPS

// Store the textContent from the $h1

// Apply new textContent to the .h1copy

// Apply opacity: 0 to .h1copy

// Apply opacity: 1 to .h1copy

// Determine when the h1 is off the top of the window
  // A) Manually scroll, store the window.scrollY